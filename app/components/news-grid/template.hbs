
<div class="Media-forcedHeight">
	{{#masonry-grid}}
		{{#each items as |story|}}
			<div class="Masonry-item">
				{{#link-to "line-up.artist" story}}
					{{teaser-item item=story}}
				{{/link-to}}
			</div>
		{{/each}}
	{{/masonry-grid}}
</div>

{{yield}}